# GENERATED BY CHEF
# Do not modify this file. Your changes will be overwritten.

lvm = <%= @defaults["volume_group"] %>
install-method = <%= @defaults["install_method"] %> 
install-source = <%= @defaults["install_source"] %>

##
#  Disk and Sizing options.
##
size   = <%= @defaults["size"] %>   # Disk image size.
memory = <%= @defaults["memory"] %> # Memory size
noswap = 1                          # Don't use swap at all for the new system.
fs     = <%= @defaults["fs"] %>     # use the EXT3 filesystem for the disk image.
dist   = sid                        # Default distribution to install.
image  = sparse                     # Specify sparse vs. full disk images.

##
# Networking setup values.
##
gateway   = <%= @defaults["gateway"] %>
netmask   = <%= @defaults["netmask"] %>
broadcast = <%= @defaults["broadcast"] %>
ip        = <%= @defaults["ip"] %>

##
# Misc options
##

#
# Uncomment the following line if you wish to interactively setup
# a new root password for images.
passwd = 0 # This requires STDIN, which we don't have. 

# Default kernel and ramdisk to use for the virtual servers
kernel = <%= @defaults["kernel"] %> 
initrd = <%= @defaults["initrd"] %>

#  The architecture to use when using debootstrap, rinse, or rpmstrap.
arch=amd64

# The default mirror for debootstrap to install Debian-derived distributions
#mirror = ftp://mir1.ovh.net/debian/
mirror = http://ftp.debian.org/debian/

# Filesystem options for the different filesystems we support.
ext3_options   = noatime,nodiratime,errors=remount-ro
ext2_options   = noatime,nodiratime,errors=remount-ro
xfs_options    = defaults
reiser_options = defaults

# Don't add an extension to vm config, so I can do xm create machine
extension   = 
